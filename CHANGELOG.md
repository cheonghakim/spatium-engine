# Changelog

이 문서는 Spatium Studio(실내 지도 편집기)의 주요 변경 사항을 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/)를 따릅니다.

## [Unreleased]

### 스튜디오 UI 전면 개편

프로토타입 느낌의 네온/고대비 UI를 AutoCAD·Figma·Blender류의 전문 데스크톱 설계 도구에
가까운 형태로 다시 디자인했습니다. 기존 기능·데이터 구조·편집 동작은 변경하지 않았습니다.

- **디자인 토큰 재정의** (`apps/studio/src/styles/tokens.css`): 차콜/슬레이트 계열 배경, 절제된
  인디고 액센트(`#6675E8`) 중심의 의미 기반 색상 체계, 4/8px 간격 스케일, 절제된 반경·그림자.
  버튼 우선순위(Primary/Secondary/Ghost)를 명확히 구분.
- **상단 툴바**: 프로젝트 / 편집 / 보기·실행 세 영역으로 구분. 실행 취소·다시 실행·전체 지우기는
  Ghost 아이콘 버튼으로, 내보내기만 Primary 버튼으로 강조.
- **좌측 도구 패널**: 아이콘만 나열하던 방식을 "선택 및 탐색 / 도형 작성 / 출입구 / POI 및 객체 /
  내비게이션 / 측정 및 보정 / 자동 감지" 그룹으로 재구성. 각 도구에 아이콘+이름+단축키를 함께
  표시하고, 패널이 좁아지면 아이콘 전용 모드로 자동 전환.
- **레이어 패널**: 체크박스 목록 대신 눈 아이콘 표시/숨김 토글, 레이어별 색상 인디케이터, 검색
  기능을 갖춘 형태로 재구성.
- **속성 패널**: 선택 항목이 없을 때 현재 층 정보·객체 수·저장 상태·검증 요약을 보여주는 대시보드로
  개편. 선택된 객체는 "기본 정보 / 위치 및 크기 / 스타일 / 연결 및 네비게이션" 등으로 그룹화하고,
  숫자 입력에는 단위 표시와 증감 버튼(`NumberField.vue`)을 적용.
- **하단 검증 패널**: 항상 펼쳐져 있던 목록을 접힌 상태 기본 + 오류/경고 개수 배지로 변경. 펼치면
  우선순위순 목록이 나오며, 항목 클릭 시 해당 객체로 캔버스 이동. 오류 문구를 기술적 코드 대신
  자연스러운 한국어 문장으로 번역(예: "공간에 유형이 지정되지 않았습니다.").
- **캔버스**: 저대비 2단(1m/5m) 격자, 통일된 선택 핸들 스타일, 줌 배율·격자 간격·스냅 상태를
  보여주는 우측 하단 플로팅 컨트롤, 좌측 상단 현재 도구 안내 오버레이 추가.
- 패널 너비 드래그 리사이즈 + 접기/펼치기(아이콘 레일) 지원, 상태는 로컬 저장소에 유지.

### 가구(Furniture) 배치

- 새 데이터 타입 `Furniture`와 `FURNITURE_PRESETS` 카탈로그 도입 (`packages/core`). 기존 프로젝트
  파일과 완전히 하위 호환(스키마 버전 변경 없이 `furniture: []`로 자동 보정).
- 좌측 도구에 **가구** 도구 추가(단축키 `F`) — 클릭으로 배치, 속성 패널에서 종류·치수·회전 조정.
- 기본 프리셋(가정용): 책상 · 의자 · 소파 · 테이블 · 수납장 · 침대.
- **사무용 가구 프리셋 추가**: 회의 테이블 · 사무용 의자 · 책장 · 서류 캐비닛 · 파티션 · 안내 데스크.
  각 항목은 절차적 3D 박스 모델(휠 베이스 사무용 의자, 개방형 책장, 서랍형 캐비닛 등)로 렌더링.
- **가구 라이브러리 패널**: 프리셋 그리드에서 클릭 한 번으로 배치할 종류를 고르고, 배치한 가구의
  치수·회전을 조정한 뒤 "내 가구"로 저장해 다른 프로젝트에서도 재사용 가능(브라우저 저장소 기반).
- **GLB 모델 가져오기**: 텍스처가 포함된 GLB 파일(최대 2MB)을 가구로 추가해 프로젝트 JSON에
  base64로 함께 저장 — 외부 파일 의존성 없이 프로젝트를 통째로 공유·복원 가능.
- 가구를 드래그로 옮기면 진입한 공간을 자동으로 감지해 `spaceId`를 갱신.
- 검증 규칙 추가: 가구가 존재하지 않는 공간을 참조하면 오류로 표시.

### 객체 그룹(Group) & 라벨링

- 여러 객체(공간·벽·출입구·POI·가구·내비게이션 노드 등, 종류 무관)를 선택한 뒤 이름이 있는
  **그룹**으로 묶는 기능 추가.
- 상단 툴바의 **그룹으로 묶기**(`Ctrl/⌘+G`) / **그룹 해제**(`Ctrl/⌘+Shift+G`) 버튼.
- 그룹 동작은 Figma/AutoCAD 방식: 구성 요소를 클릭하면 그룹 전체가 선택되고(캔버스에 점선
  외곽선 + 이름표 표시), 드래그하면 모든 구성 요소가 함께 이동하며, Delete는 그룹과 모든 구성
  요소를 한 번에 삭제(그룹 해제는 별도 동작으로 구성 요소를 보존).
- 개별 구성 요소를 단독으로 삭제하면 그룹 목록에서 자동으로 제외되고, 남은 구성 요소가 1개
  이하가 되면 그룹이 자동 해제됨.
- 속성 패널에 그룹 이름 편집 및 구성 요소 목록 표시, 레이어 패널에 그룹 목록 섹션 추가.
- 기존 프로젝트 파일과 하위 호환(`groups: []` 자동 보정, 스키마 버전 변경 없음).

### 수정된 문제

- **경로 그리기 중 Shift 정렬 미지원**: 벽 그리기 도구에는 있던 "Shift: 수평/수직 정렬"이
  내비게이션(경로) 도구에는 없었던 문제를 수정. 이제 경로를 그릴 때도 Shift를 누른 채 클릭하면
  새 지점이 이전 지점과 수평 또는 수직으로 정렬됩니다. (참고: 격자 맞춤 토글 자체는 이 문제와
  무관하게 이전부터 정상 동작했습니다.)
- **속성 패널에 삭제 버튼 추가**: 선택한 객체를 Delete 키로만 지울 수 있던 것을 개선해, 속성
  패널 상단에도 삭제 버튼(휴지통 아이콘)을 추가. Delete 키와 동일한 삭제 로직(그룹 구성 요소
  정리, 교차 참조 해제 등)을 공유하며, 실행 취소(Ctrl/⌘+Z)로 되돌릴 수 있습니다.
